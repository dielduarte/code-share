{
  "name": "code-share",
  "displayName": "code-share",
  "description": "an extension to share your project or especific file from vscode to world",
  "version": "0.0.1",
  "publisher": "code-share",
  "engines": {
    "vscode": "^1.27.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:codeshare.shareProject"
  ],
  "main": "./out/extension",
  "contributes": {
    "commands": [
      {
        "command": "codeshare.shareProject",
        "title": "Share Project - CodeSandBox"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test",
    "prettier": "prettier --write ./src/**/*.ts"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.42",
    "@types/node": "^8.10.25",
    "tslint": "^5.8.0",
    "typescript": "^3.1.1",
    "vscode": "^1.1.21"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "codesandbox": "^1.3.0",
    "codesandbox-import-utils": "^1.3.0",
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
